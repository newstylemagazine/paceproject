# Copy this file to .env and add your API key.
# The site will still run without a key, but AI responses will use fallback mode.

OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
PORT=8000
